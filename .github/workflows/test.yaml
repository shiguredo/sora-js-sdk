name: test

on:
  push:
    paths-ignore:
      - "**.md"
      - "LICENSE"
      - "NOTICE"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: ["16", "18", "20"]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - uses: pnpm/action-setup@v2
        with:
          version: 8
      - run: pnpm --version
      - run: pnpm install
      - run: pnpm -r --filter=./packages/e2ee build
      - run: pnpm -r --filter=./packages/sdk build
      - run: pnpm -r --filter=./packages/e2ee lint
      - run: pnpm -r --filter=./packages/sdk lint
      - run: pnpm -r --filter=./packages/sdk test
      - run: git diff --stat --exit-code
      - name: Slack Notification
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: sora-js-sdk
          SLACK_COLOR: danger
          SLACK_TITLE: Failure test
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
